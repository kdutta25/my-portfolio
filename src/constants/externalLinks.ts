/** Résumé PDF served from `public/` (same-origin, opens in new tab from nav). */
export const RESUME_PDF_URL = `${import.meta.env.BASE_URL}Kaustubh-Dutta-Resume.pdf`;

/** Repository root on GitHub — README is shown below the file tree (opens in new tab from nav). */
export const README_ON_GITHUB_URL = "https://github.com/kdutta25/my-portfolio";
